#include <stdio.h>
/**
 * main - calculate the factorial of a given number
 * Return: 0
 */
int main(void)
{
	int n = 5;
	int i;
	int factorial = 1;

	for (i = 1; i <= n; i++)
	{
		factorial *= 1
	}
	printf("The factorial of %d is: %d\n", n, factorial);
	return (0);
}
